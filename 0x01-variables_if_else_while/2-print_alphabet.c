#include <stdio.h>

/**
 * main - A program that prints alphabets in lowercase
 * Return: 0 (Excellent)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);

}
